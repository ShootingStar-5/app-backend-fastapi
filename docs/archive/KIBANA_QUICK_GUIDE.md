# Kibana 대시보드 5분 만에 만들기

## 🚀 빠른 시작

### 1단계: Kibana 접속 (10초)
```
http://localhost:5601
```

### 2단계: 자동 대시보드 생성 (1분)
```bash
# 기본 대시보드
python scripts/setup_kibana_dashboard.py

# 고급 대시보드 (더 많은 시각화)
python scripts/create_advanced_dashboard.py
```

### 3단계: Kibana에서 확인 (3분)

1. **Visualize Library** 클릭
2. 생성된 시각화 확인:
   - ✅ 전체 제품 수
   - ✅ 카테고리별 분포
   - ✅ 제조사 랭킹
   - ✅ 월별 추이
   - ✅ 기능성 분포

3. **Dashboard** → **Create dashboard** 클릭
4. **Add** 버튼으로 시각화 추가
5. 레이아웃 조정 후 **Save**

---

## 📊 주요 Dev Tools 쿼리

### 1. 전체 제품 수 확인
```json
GET /health_supplements/_count
```

### 2. 카테고리별 집계
```json
POST /health_supplements/_search
{
  "size": 0,
  "aggs": {
    "categories": {
      "terms": {
        "field": "classification.category.keyword",
        "size": 20
      }
    }
  }
}
```

### 3. 제조사 TOP 10
```json
POST /health_supplements/_search
{
  "size": 0,
  "aggs": {
    "top_companies": {
      "terms": {
        "field": "company_name.keyword",
        "size": 10,
        "order": { "_count": "desc" }
      }
    }
  }
}
```

### 4. 최근 등록 제품 10개
```json
POST /health_supplements/_search
{
  "size": 10,
  "sort": [
    { "report_date": "desc" }
  ],
  "_source": ["product_name", "company_name", "report_date"]
}
```

### 5. 특정 성분 검색
```json
POST /health_supplements/_search
{
  "query": {
    "match": {
      "raw_materials": "비타민D"
    }
  },
  "size": 5
}
```

---

## 🎨 추천 시각화 조합

### 대시보드 A: 제품 현황
```
┌─────────────┬─────────────┐
│ 전체 제품 수 │ 카테고리 분포│
│  (Metric)   │   (Pie)     │
├─────────────┴─────────────┤
│   제조사 TOP 20            │
│  (Horizontal Bar)         │
└───────────────────────────┘
```

### 대시보드 B: 트렌드 분석
```
┌───────────────────────────┐
│   월별 등록 추이           │
│   (Line Chart)            │
├───────────────────────────┤
│   기능성 내용 분포         │
│   (Donut Chart)           │
└───────────────────────────┘
```

---

## 💡 유용한 팁

### 빠른 필터 추가
Dashboard 편집 → **Controls** → **Options list**
- Field: `classification.category.keyword`
- Label: "카테고리 선택"

### 시간 범위 설정
우측 상단 달력 아이콘 → 원하는 기간 선택

### 데이터 새로고침
좌측 상단 **Refresh** 버튼 또는 자동 새로고침 설정

### 대시보드 공유
**Share** → **Copy link** → URL 복사

---

## 🔍 자주 사용하는 검색

### Discover 탭에서:

**1. 특정 제조사 제품 찾기**
```
company_name:"원하는 제조사명"
```

**2. 여러 조건 검색**
```
classification.category:"건강기능식품" AND raw_materials:"오메가3"
```

**3. 날짜 범위 검색**
```
report_date:[2024-01-01 TO 2024-12-31]
```

**4. NOT 조건**
```
NOT company_name:"제외할 제조사"
```

---

## 📈 시각화 타입별 추천 용도

| 시각화 타입 | 추천 용도 | 필드 예시 |
|------------|----------|----------|
| **Metric** | 단일 숫자 표시 | 전체 제품 수 |
| **Pie/Donut** | 비율 표시 | 카테고리 분포 |
| **Bar/Column** | 순위 비교 | 제조사 TOP 10 |
| **Line** | 시간 추이 | 월별 등록 추이 |
| **Data Table** | 상세 목록 | 최근 제품 리스트 |
| **Tag Cloud** | 빈도 시각화 | 주요 성분 |
| **Heatmap** | 2차원 분포 | 카테고리×세부카테고리 |

---

## 🛠️ 문제 해결

**시각화가 비어있을 때:**
1. Time range 확인 (전체 범위로 설정)
2. Index pattern 확인
3. 필터 제거

**데이터가 보이지 않을 때:**
```bash
# Elasticsearch에 데이터가 있는지 확인
curl http://localhost:9200/health_supplements/_count
```

**시각화 생성 실패:**
```bash
# Kibana 재시작
docker restart health-supplement-kibana
```

---

## 📚 더 알아보기

- [상세 대시보드 가이드](KIBANA_DASHBOARD_EXAMPLES.md)
- [Kibana 공식 문서](https://www.elastic.co/guide/en/kibana/current/dashboard.html)
- [Elasticsearch 쿼리 DSL](https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl.html)

---

**5분 안에 완성! 이제 데이터를 시각화하고 분석하세요! 📊**
